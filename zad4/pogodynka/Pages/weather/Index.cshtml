@page
@model pogodynka.Pages_weather.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].weatherDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].mainWeatherTemp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].visibility)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].dt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].timezone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weather[0].cod)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Weather) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.weatherDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mainWeatherTemp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.visibility)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.timezone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cod)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
